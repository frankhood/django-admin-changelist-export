============
Installation
============

At the command line::

    $ easy_install django-admin-changelist-export

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-admin-changelist-export
    $ pip install django-admin-changelist-export
